install.packages(c("knitr", "tidyverse"))

# Demo-Factor.R
install.packages(c("stats", "car", "psych"))

# Demo-TimeSeries.R
install.packages(c("TTR", "forecast", "tseries"))

# Demo-Survival.R
install.packages(c("survival", "survminer"))

# call libraries
library(knitr)
library(tidyverse)

library(stats)
library(car)
library(psych)
library(TTR)

library(forecast)
library(tseries)

library(survival)
library(survminer)